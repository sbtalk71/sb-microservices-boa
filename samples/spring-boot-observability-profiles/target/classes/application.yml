spring:
  application:
    name: spring-boot-observability
  profiles:
    active: splunk-uf

management:
  endpoints:
    web:
      exposure:
        include: "*"
  observations:
    annotations:
      enabled: true
